# Django Source Code

All Django-related code lives inside this `src/` folder. It contains the
project configuration, custom apps, HTML templates, static files, and the
main `manage.py` entry point for running the server.

To run the project:
1. cd src
2. source env/bin/activate
3. python manage.py runserver

